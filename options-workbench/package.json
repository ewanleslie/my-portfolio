{
  "name": "options-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "Options Workbench — payoff & Greeks explorer, derivatives quizzes, and a multi-leg strategy builder.",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --minify --jsx=automatic --define:process.env.NODE_ENV=\\\"production\\\" --outfile=bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
