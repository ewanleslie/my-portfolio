{
  "name": "accounting-trainer-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Three-Statement Studio / Accounting Trainer Tool — interactive model explorer and quizzes.",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --minify --jsx=automatic --define:process.env.NODE_ENV=\\\"production\\\" --outfile=bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
